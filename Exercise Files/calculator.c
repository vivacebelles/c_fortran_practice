#include <stdio.h>
#include <math.h>

int main()
{
    struct calculator{
        float factor1;
        float factor2;
        float factor3;
    };
    return(0);
}

int math()
{
    struct calculator{
        printf("%n * %n", factor1, factor2);
    };
    return(0);
};
